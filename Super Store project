  IKEA

—SELECT statement to order the items by price.
SELECT item_name,price
FROM superstore 
ORDER BY price ASC;



2.#Show a statistic about the item prices.
SELECT AVG (price)
FROM superstore;



	3.#Show a statistic about the price for items in the category of "Kitchen Supplies
             SELECT AVG(price)
             FROM superstore 
            WHERE category= 'Kitchen Supplies'



          4. #Show the lowest rated item by category	
            SELECT MIN(average_rating),category
           FROM superstore
          WHERE stock_quantity
           GROUP by category asc;


          5. .#Show most priced category
SELECT MAX(price), category
           FROM superstore 
           GROUP BY category
            LIMIT 4;
